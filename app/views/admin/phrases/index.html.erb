<% if @phrases %>
  <ul>
  <% @phrases.each do |phrase| %>
    <li><%= link_to("Character #{phrase.value}", admin_character_path(phrase)) %></li>
  <% end %>
  </ul>
<% end%>

<%= link_to "Create new phrase", new_admin_phrase_path %>
